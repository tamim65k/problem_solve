#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int t; cin>>t;
    while(t--){
        int a(0),b(0);
        string s; cin>>s;
 
        for(int i=0;i<3;i++) a+= s[i] -'0';
        for(int i=3;i<6;i++) b+= s[i] - '0';
        cout<<(a==b? "YES
": "NO
");
    }
    return 0;
}